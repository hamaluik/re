# re

Random Github emoji on the command line.

1. Build:
    ```
    cargo build --release
    ```
2. Install:
    ```
    cp ./target/release/re ~/bin/
    ```
3. Use:
    ```
    $ re
    :balloon:
    ```
    ```
    $ git commit -m "$(re)"
    ```

